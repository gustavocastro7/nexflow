# Server
PORT=3000
NODE_ENV=production

# PostgreSQL (fornecido pelo Hostinger)
DB_HOST=localhost
DB_NAME=nexflow_db
DB_USER=nexflow_user
DB_PASS=nexflow_password

# JWT
JWT_SECRET=change_this_to_a_random_secret_key

# Admin default (opcional - cria automaticamente se nao existir)
ADMIN_EMAIL=gustavocastro73@gmail.com
ADMIN_PASSWORD=castro
