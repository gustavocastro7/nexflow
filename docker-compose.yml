services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nexflow-app
    env_file:
      - .env
    environment:
      NODE_ENV: production
    ports:
      - "3002:3000"
    restart: unless-stopped
